import { openrouter as baseOpenRouter } from '@openrouter/ai-sdk-provider';

/**
 * OpenRouter provider configured with Tiger.Chat API key
 * The API key is fetched from environment variables
 */
export const openrouter = baseOpenRouter;
